# Excel Layout Notes
- One worksheet (e.g., `brick_strength_raw`)
- Columns in this exact order:
  `table_id`, `test_date`, `instrument_id`, `brick_type`, `strength_grade`,
  `seq`, `test_location`, `estimated_strength_mpa`
- Each row corresponds to one entry in `rows`
